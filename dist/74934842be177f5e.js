import*as THREE from"three";import{camera,scene,renderer}from"./basics";const axesHelper=new THREE.AxesHelper(2e3);axesHelper.setColors("green","purple","orange"),scene.add(axesHelper),window.addEventListener("resize",(e=>{camera.aspect=window.innerWidth/window.innerHeight,camera.updateProjectionMatrix(),renderer.setSize(window.innerWidth,window.innerHeight),renderer.setPixelRatio(Math.min(window.devicePixelRatio,2))}));const clock=new THREE.Clock;function render(e){const r=clock.getElapsedTime();sphere.rotation.x=.5*r,cube.rotation.x=.5*r,torus.rotation.x=.5*r,spotHelper.update(),controls.update(),window.requestAnimationFrame(render),renderer.render(scene,camera)}render();